# Copy to .env.docker.prod and update values for the Docker production stack.
# This file is safe to commit; the real .env.docker.prod file is ignored.

API_BASE_URL=http://gateway-service:8080
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_GATEWAY_WS_URL=ws://localhost:8080/ws
