<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="serviceName" source="spring.application.name" defaultValue="notification-service"/>
    <springProfile name="dev,local,test">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender"><encoder><pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%thread] %logger{36} service=${serviceName} requestId=%X{requestId:-} userId=%X{userId:-} - %msg%n%ex</pattern></encoder></appender>
    </springProfile>
    <springProfile name="!dev &amp; !local &amp; !test">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender"><encoder class="net.logstash.logback.encoder.LogstashEncoder"><timeZone>UTC</timeZone><customFields>{"service":"${serviceName}"}</customFields><includeMdc>true</includeMdc><fieldNames><timestamp>timestamp</timestamp><level>level</level><thread>thread</thread><logger>logger</logger><message>message</message><stackTrace>stack_trace</stackTrace></fieldNames></encoder></appender>
    </springProfile>
    <root level="${LOG_LEVEL:-INFO}"><appender-ref ref="CONSOLE"/></root>
</configuration>
